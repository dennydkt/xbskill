# 日期：2026-08-09

陌生上下文声明：本次以首次接触该系统、不了解作者意图与验收标准的陌生用户代理身份，只按当前 `xbskill` 字面入口及正常运行明确要求的引用与 `xb-automation` 专科处理原始问题。

未读声明：未读取 `specialist-regression.md`、`specialist-rewrite-method.md`、`dbs-reuse-case.md`、`system-design.md`、`v0.5-forward-test-record.md`，未读取任何既有 `v0.5-*-raw` 文件或其他独立测试/评分材料；目标文件写入前不存在。

原文状态：以下为生成时的自然回答原文，未作修改。

---

> xbskill（快点下班 Skill）是一套站在脑力工作者一侧的工作生存与选择系统。你可以直接交给我一个模糊任务、一段材料、一个难沟通的人、一项不会的软件操作，或一件让你耗尽却推进不了的事。
>
> 我会先判断你现在需要稳住、解卡、看懂、改变还是选择；再结合目标、状态、能力、相关同事和领导、公司环境，判断这件事应该解决、调整，还是停止、拒绝、升级、转岗或离开。
>
> 它不承诺工作零痛苦，也不会用心理疏导教你继续忍受结构性伤害。目标是减少无谓消耗，完成必要工作，并让你的边界和选择越来越清楚。
>
> 你不需要记住 Skill 名称，也不用把问题整理好。把最近最想处理的一件工作直接发过来即可。

这个目标值得自动化，但现在不能直接把真实客户名单交给 AI 并自动外发。公司“没有明确 AI 规定”只说明规则未知，不等于工具、客户数据和发送动作已经获批。

当前按 G2/H2，选择门为“调整”，先用 `$xb-automation` 处理许可、可控性和实测净收益这个瓶颈。保留自动化方向，把“一次性全自动”改成“先生成待发送预览，经过授权和实测后再逐级开放发送”。

建议把目标流程设计成这个最小闭环：

1. 从获准的数据源读取名单，只取完成邮件所需的最少字段；同时读取排除名单、退订/不可联系状态和模板版本。
2. 清洗、校验、去重、匹配模板等确定性步骤优先用规则或脚本。只有确实需要分类、摘要或个性化草拟时才让 AI 参与，而且只给它获准的最少字段；若规则足够固定，就不必使用 AI。
3. 先生成“待发送预览”，逐条显示收件人、使用依据、模板版本、个性化内容、异常原因和“禁止发送”标记，不直接触发真实邮箱。
4. 人工确认批次和异常项后，才由获准的邮箱/API 发送。用“批次 ID + 客户 ID + 模板版本”做幂等键，防止重跑造成重复发送；保留发送、失败、修改和审批日志。
5. 任何字段缺失、名单冲突、权限失效、模型输出越界或结果无法验收时，流程应响亮停下并转人工，不能猜测补全，也不能盲目重试。

推进顺序不能跳级：

- **虚构样本**：只用虚构姓名、虚构业务内容和测试地址，覆盖正常、边界、缺字段、重复、错误、负例和中断恢复；不上传真实名单，不连接真实外发。
- **影子对比**：在工具和数据使用已获允许的前提下，用获准的脱敏或历史样本与人工流程并行，只产出预览，不发信；逐项比较漏项、错项、重复和人工修改。如果数据使用尚未获批，就继续停留在虚构沙盒。
- **内部测试**：在获批环境中只发到明确指定的内部测试邮箱，验证重复运行、停止、恢复、告警、日志和人工接管。
- **获批小批量**：工具、数据和真实发送另行授权后，冻结名单、模板和范围，每批仍由人确认，再向获批的小范围真实对象发送。
- **扩大或无人值守**：只有许可门和净收益门都通过，且有负责人、监控、停用和接管机制，才讨论扩大；“直接自动外发”始终是独立授权项，不因前面跑通而自动获得许可。

许可门需要有实际权力的人明确确认，至少包括：

- 哪个 AI/自动化工具、模型和运行环境可以使用；
- 哪些客户字段可以被读取、传输和写入日志，数据如何留存、删除和访问；
- 这批客户的联系依据、用途、排除/退订规则由谁确认；
- 哪个账号可以发送、谁批准收件范围与模板、谁承担复核和异常处置；
- 出错时谁能立刻停用、撤销凭据、处置泄露或错发。

实际岗位名称因公司而异，通常需要流程负责人、数据负责人、信息安全/隐私或合规负责人、邮件系统/账号负责人中真正有决定权的人确认。若公司没有成文 AI 规则，就请他们对这个具体试点作书面决定；不要让你个人用“没人反对”替组织承担风险。

净收益门要与许可门分开。现在唯一已知的基线是你自述“每天约两小时”，还不足以宣布全自动值得上线。观察窗内要记录：

| 字段 | 当前状态 |
|---|---|
| 人工基线时间、波动、返工和例外 | 约两小时/日为用户报告；分布待采 |
| AI/脚本运行时间 | 待测 |
| 人工复核与纠错时间 | 待测 |
| 设置与维护时间的摊销 | 预测后实测 |
| 错误数、类型及单次后果 | 待测 |
| 漏发、错发、重复发送和异常接管次数 | 待测 |
| 实际采用率 | 待测 |
| 实测净节省 | 待测；不能用理论节省代替 |

在看到试跑结果前，由流程负责人和承担风险且有权扩大的人共同冻结一张阈值依据卡：指标按批次、收件人、字段还是工作日计算；样本和观察窗要覆盖哪些正常、边界、峰值与规则变化；人工基线如何波动；错误怎样分级、单次后果由谁承担；最低净收益是多少；阈值来自安全硬约束、正式制度、历史基线还是责任人的风险偏好；谁批准、何时冻结；什么变化会触发重估。

这里可以先确定一条安全硬约束：把“未获批真实外发、发给错误或被排除的收件人、泄露敏感字段、重复真实发送”定义为关键错误，关键错误必须为 0。非关键错误率、采用率、最低净节省、观察天数和样本量目前都只能标“待校准”，我不会凭空写一个百分比或天数。观察窗必须覆盖预先约定的代表性场景和最小暴露量，不能只因为过了几天就算通过。

扩大条件是：许可项全部有明确批准，关键错误为 0，并且复核时间、维护成本、采用率和实测净节省达到事前冻结的业务阈值。停止或降级条件是：出现任何关键错误；权限或联系依据变得不清楚；错误无法及时发现或回滚；规则变化、例外或维护负担超过冻结阈值；人工复核加维护已经吃掉节省。停止后退回待发送预览、纯规则脚本或人工流程，不用继续给无人值守系统打补丁。

要开始搭建，请先只提供这些非敏感材料，不要发真实客户名单：

- 你们使用的邮件系统，以及是否存在管理员批准的自动化接口；
- 名单的字段名和覆盖正常、边界、错误情况的虚构样本；
- 你现在从收到名单到发出邮件的每一步、判断点和常见例外；
- 邮件模板与哪些字段需要变化，内容全部用虚构值；
- 谁能分别批准工具、数据、留存、联系依据和真实发送；
- 若干次人工运行的耗时、返工、漏项和异常记录。

拿到这些后，当前能安全交付的是一个不接触真实客户、不真实外发的 dry-run 流程、预览表和测试矩阵；实际发送开关仍单独等待授权。若最终拿不到 AI 或客户数据使用许可，就把方案翻转为本地确定性清洗与邮件草稿生成，保留人工发送；若连真实发送授权也没有，就只做到预览。

本轮已经交付自动化蓝图、许可门、实测字段和阶段条件，但现实中的两小时重复劳动尚未减少，不能宣布问题已解决。下一反馈点是：有权者对工具、数据和发送作出明确决定，并完成代表性影子运行；届时再用关键错误、复核/维护成本、采用率和实测净节省决定扩大、降级或停止。
