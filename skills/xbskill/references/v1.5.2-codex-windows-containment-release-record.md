# v1.5.2 Codex Windows 会话历史隔离发布记录

```text
Validated-At: 2026-08-19
Version: xbskill 1.5.1 → 1.5.2
Baseline: source 41e8827a3341960a01e9499c40f3896205900b25；memory/ 为用户本地会话档案，本轮排除
Change-Type: 会话完整性恢复安全门修复，公开专科数 35 不变
Result: 工程门通过；Codex Windows 自动历史恢复已隔离；客户端原生链路仍由客户端版本决定
```

## 基线与回滚点

- 源码基线：`41e8827a3341960a01e9499c40f3896205900b25`，版本 1.5.1。
- 公共兼容副本基线：workspace `c9d29ce08b0bc1ec72f1f476990b13f7ebb6f973`。
- 源码工作树已有未跟踪 `memory/`，本轮读取、同步与提交范围均排除。
- 本机 Codex 状态库完整性为 `ok`；4 条任务路径带 Windows 扩展前缀。经用户授权只修复其中 3 条，另 1 条保持原样。
- 任务数据备份：`%USERPROFILE%\.codex\backups\thread-repair-three-confirmed-20260819-011527`。

## 本轮范围 / 排除项

范围：

1. Windows 版 Codex Desktop 的压缩恢复走安全分支，禁用任务历史与任务生命周期接口。
2. 明确禁止寻找“原始任务”、关联任务或推断出的压缩前任务。
3. Codex 安全分支只接受当前可见上下文与用户主动提供的完整导出；有缺口时提供“完整导出”或“保存现有部分”选项。
4. 其他宿主保留受约束的纯只读恢复：精确当前会话 ID、无生命周期副作用、全分页和完整性核对全部通过后才可标 `complete`。
5. 将宿主分支、禁用原因、已尝试入口和缺口纳入响亮失败信息。

排除项：

- 普通 xbskill 运行不得读取或修改 Codex 私有数据库、session/rollout 文件与归档目录。
- 不修复 Codex 客户端自身的 Windows 路径生命周期实现。
- 不把任务摘要、压缩摘要、隐藏内容、截断消息或不可访问附件补成逐字正文。
- 不读取其他任务，不扩大保存授权，不联网外发。

## 正常、反例与边界样本

| 样本 | 条件 | 预期 |
|---|---|---|
| 正常：Codex Windows 上下文完整 | 当前可见上下文覆盖全部轮次 | 不调用任务接口，直接进入本地保存完整性核对 |
| 正常：其他宿主纯只读 | 精确当前会话 ID 来自宿主；接口无生命周期副作用；分页到头 | 只读当前会话并核对，全部通过后可标 `complete` |
| 反例：尝试寻找原始任务 | 当前上下文有压缩摘要，标题或内容可匹配其他任务 | 禁止跨任务搜索，报告安全分支与缺口 |
| 边界：Codex Windows 上下文不完整 | 压缩前轮次或附件缺失 | 禁用任务接口，请用户提供完整导出或接受部分保存 |
| 边界：接口副作用未知 | 宿主声称可读历史，但加载、恢复或生命周期行为不清楚 | 按入口不可用处理，禁止调用 |

## 八门与完成边界

| 门 | 本轮证据目标 | 状态 |
|---|---|---|
| G | 用户使用 xbskill 时不触发已观察到的 Codex Windows 任务异常链 | 2 |
| C | 宿主识别、安全分支、允许分支、完整性缺口形成闭环 | 2 |
| A | 读取历史、保存本地和任务生命周期权限分开 | 2 |
| P | 与人物、权力和组织判断无关 | 2 |
| S | Codex 私有状态与跨任务读取均有硬停止门 | 2 |
| E | 正常、反例和未知副作用样本可静态检查 | 2 |
| R | 完整上下文可保存；缺口转为用户可选的导出或部分保存 | 2 |
| V | 干净接收方、forward test 与隐藏性校验 | 2 |

## 验证证据

- 源码 `suite_manager.py validate`：35 个公开 Skill，0 error、0 warning。
- 干净接收方 `forward_test.py`：32/32 通过；安全分支、事务保存、缺依赖响亮失败和既有回归均保留。
- 兼容副本：版本 1.5.2，与源码 0 added、0 modified、0 removed；`LOCAL-*` 保留 1 项。
- 隐藏性校验：公开 Skill 35 个，内部专科无 UI、manifest 或公共桥泄漏，盲测 2/2。
- 六个宿主 Skill 根：`DuplicateNames=0`。
- 用户授权的 3 个故障任务：归档 3/3 成功，取消归档 3/3 成功；最终 `archived=0`、JSONL 存在、路径无 Windows 扩展前缀。
- 排除的第 4 个任务：路径、归档状态与文件均未变化；最终全库仅保留该 1 条扩展前缀。
- 修复后 SQLite `pragma integrity_check=ok`。
- 本轮修改文案的禁用句式特征串：0 命中。

完成范围只覆盖 xbskill 对宿主历史能力的调用边界。Codex 客户端原生对话、加载与归档仍由客户端版本决定。
